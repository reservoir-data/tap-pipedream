send_anonymous_usage_stats: false
requires_meltano: ">=4.0"
project_id: tap-pipedream
default_environment: dev
environments:
- name: dev
plugins:
  extractors:
  - name: tap-pipedream
    label: Pipedream
    namespace: pipedream
    description: Pipedream is an integration platform for developers.
    repo: https://github.com/reservoir-data/tap-pipedream
    docs: https://github.com/reservoir-data/tap-pipedream#readme
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
    - structured-logging
    settings:
    - name: token
      kind: password
      label: API Token
      description: API Token for Pipedream
    - name: organizations
      kind: array
      label: Organizations
      description: List of organization IDs to get data from
    config:
      organizations: ["o_DZIqGy"]
  loaders:
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
